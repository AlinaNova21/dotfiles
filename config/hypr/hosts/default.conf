# Default host config (fallback for unknown hosts)

# Auto-detect monitors
monitor=,preferred,auto,auto

# Programs
$terminal = ghostty
$fileManager = dolphin
$menu = fuzzel

# Monitor variables (single monitor setup)
$primaryMonitor =
$secondaryMonitor =
