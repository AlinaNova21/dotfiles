[tools]
gh = "latest"
gitui = "latest"
jq = "latest"
just = "latest"
ripgrep = "latest"
yq = "latest"

# Project-scoped tools stay in each project's mise.toml, NOT global:
# kubernetes (home-ops/dn42/tf-home-ops pin exact versions), docker, pulumi, d2, claude-code,
# op-as-tool (system-managed, see 70-packages), incus, herdr, pi, rokit, uv, bun, secret scanners.